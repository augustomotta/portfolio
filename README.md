# Portfólio - Carlos Augusto Rolim da Motta

Portfólio profissional desenvolvido com HTML, CSS e JavaScript vanilla.

## 🚀 Como usar

1. Clone o repositório
2. Copie o arquivo `config.example.js` para `config.js`:
   ```bash
   cp config.example.js config.js
   ```
3. Edite o arquivo `config.js` e adicione seu ID do Google Analytics
4. Abra o arquivo `index.html` no navegador

## 🔒 Configuração

O arquivo `config.js` contém informações sensíveis e está no `.gitignore`. 

Para configurar:
- Edite `config.js` com seus dados reais
- Use `config.example.js` como referência

## 📦 Estrutura

```
├── index.html          # Página principal
├── styles.css          # Estilos do site
├── favicon.svg         # Ícone do site
├── config.js           # Configurações (não commitado)
└── config.example.js   # Exemplo de configuração
```

## 🛠️ Tecnologias

- HTML5
- CSS3
- JavaScript (Vanilla)
- Google Analytics (opcional)

## 📄 Licença

© 2026 Carlos Augusto Rolim da Motta
